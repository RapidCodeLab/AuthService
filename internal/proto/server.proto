syntax = "proto3";

package authgrpcserver;

option go_package = "/authgrpcserver";

service Auth{
    rpc GetPublicKey(Empty) returns (Response);
}

message Empty{}

message Response{
    bytes public_key = 1;
}